
package Bullet;


/**
 *
 * @author gajdp9323
 */
public class Bullet {
    int nX = 300;
    int nY = 300;
    
    Image imgBullet = "Bullet.png";
    double rotationRequired = Math.toRadians (45);
    double locationX = image.getWidth() / 2;
    double locationY = image.getHeight() / 2;    
}